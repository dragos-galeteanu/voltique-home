# Store listing copy

Drafts, not final copy. Nothing here is uploaded by a release: both lanes skip metadata
so that shipping a build can never overwrite a live listing by accident. Upload it
deliberately once the wording is approved.

`en-US/` is the App Store, `android/en-US/` is Google Play. The two stores have different
limits, which is why the text is not shared.
