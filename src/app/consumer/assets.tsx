import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function AssetsScreen() {
  return (
    <PlaceholderScreen
      title="Assets"
      description="Every asset in the household with its live status, plus the flow for adding one from the catalogue."
      milestone="M3"
      testID="consumer-assets"
    />
  );
}
