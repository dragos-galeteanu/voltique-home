import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function DashboardScreen() {
  return (
    <PlaceholderScreen
      title="Dashboard"
      description="Household production, consumption and battery state over time, with the charts fed by the telemetry endpoints."
      milestone="M6"
      testID="consumer-dashboard"
    />
  );
}
