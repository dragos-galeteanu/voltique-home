import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function InstallerAlertsScreen() {
  return (
    <PlaceholderScreen
      title="Alerts"
      description="Faults across every household you maintain, with the raw device logs behind each one."
      milestone="M6"
      testID="installer-alerts"
    />
  );
}
